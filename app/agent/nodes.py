from app.agent.state import GraphState

def generate_response(state: GraphState):
    # Ignore user message, hardcode response
    return {"response": "Okay, but when someone sends a new message the view still feels jarring — like the whole"}
